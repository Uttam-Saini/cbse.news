'use client';

/**
 * ThemeToggle has been disabled because dark mode has been removed.
 * This component now renders nothing to keep call sites safe if any remain.
 */
export default function ThemeToggle() {
  return null;
}

